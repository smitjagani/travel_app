import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// this basically makes it so you can't instantiate this class

  static const primaryColor = Color(0xFF0c7d7d);

  /// accentColor is a color that is used to make elements stand out
  static const accentColor = Color(0xFF6CB7FF);

  /// secondaryColor is another color that is used to make elements stand out
  static const secondaryColor = Color(0xFF878787);

  /// backgroundColor is the color of the background
  static const backgroundColor = Color(0xFFF5F5F5);

  /// scaffoldBackgroundColor is the color of the scaffold background
  static const scaffoldBackgroundColor = Color(0xFFFFFFFF);

  /// cardColor is the color of the card background
  static const textColor = Color(0xFF64748B);

  /// errorColor is the color of error messages
  static const errorColor = Color(0xFFDC2626);

  /// shadowColor is the color of shadows
  static const shadowColor = Color(0xFF64748B);

  /// dividerColor is the color of dividers
  static const dividerColor = Color(0xFFE5E5E5);

  /// successColor is the color of success messages
  static const successColor = Color(0xFF4BB543);

  /// warningColor is the color of warning messages
  static const warningColor = Color(0xFFFFA500);

  /// infoColor is the color of api_demo messages
  static const infoColor = Color(0xFF007BFF);

  /// disabledColor is the color of disabled elements
  static const disabledColor = Color(0xFF777575);
}
